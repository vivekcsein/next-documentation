import { KnowledgeHome } from "@/components/features/home/KnowledgeHome";

/** Same page as "/" — kept as its own route so links can point at either. */
const ContentPage = () => <KnowledgeHome />;

export default ContentPage;
